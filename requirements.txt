# WiFi 热点管理器 —— 依赖说明
#
# 必装：网页界面（默认界面）依赖 pywebview（Edge WebView2）
pywebview>=5.0

# 托盘功能（关闭时最小化到托盘）：pystray + Pillow
pystray>=0.19

# 1) 精确流量统计（可选）：每台设备的上行/下行抓包
scapy>=2.5.0

# 2) 抓包驱动（Windows 必装，安装时勾选 "Install Npcap in WinPcap API-compatible Mode"）
#    https://npcap.com/#download

# 3) Pillow 同时用于托盘图标绘制
Pillow>=10.0.0
